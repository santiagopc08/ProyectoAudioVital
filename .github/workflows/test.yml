name: CI - Test environment

on:
  push:
    branches: [ "master" ]
    paths:
    - "**"
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies n run tests
        run: npm install
             npm run testcoverage